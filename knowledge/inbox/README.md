# Inbox

Drop unstructured content here — links, voice transcripts, screenshots, brain dumps, anything.

Classify and file into `library/` or `work/` when you're ready. If using Claude Code, a `/tidy-knowledge` workflow can automate the classification.
